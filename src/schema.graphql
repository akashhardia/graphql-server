type Book {
  id: Int,
  title: String,
  author: String,
}

input BookInput {
  title: String,
  author: String,
}

type Query {
  books: [Book],
}

type Mutation {
  addBook(book: BookInput!): Book!,
  removeBook(id: Int!): String!,
  updateBook(id: Int!, toUpdate: BookInput!): Book!,
}
